import expect from 'chai';

describe('Sanitize', function () {
  it('returns lowercase of a string', function () {
    var word = 'hello world';

    expect(word.toUpperCase()).to.equal('HELLO WORLD');
    expect(word)
  });
  it('remove any hyphen')
})
